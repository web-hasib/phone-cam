# PhoneCam Environment Configuration

# Backend / Signaling Server URL (Defaults to Render cloud server if not set)
BACKEND_URL=https://cam-backend-4bdx.onrender.com
# For local development:
# BACKEND_URL=http://localhost:4000

# Backend & ADB Tunnel Port
PORT=4000

# Custom IP / Host overrides (Optional, auto-detected from network interface if empty)
# LOCAL_IP=192.168.0.105
# USB_HOST=127.0.0.1
